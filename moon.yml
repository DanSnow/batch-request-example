id: tanstack-starter-template
tasks:
  build:
    command:
      - vinxi
      - build
    toolchain: node
  dev:
    command:
      - vinxi
      - dev
    local: true
    toolchain: node
  start:
    command:
      - vinxi
      - start
    local: true
    toolchain: node
